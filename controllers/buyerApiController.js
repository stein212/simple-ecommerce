const dataAccess = require('../data')

const buyerApiController = {
    init(app) {
        app.post('/local/buyerApi/', function(req, res) {
            // dataAccess.
        })
    },
}

module.exports = buyerApiController
